<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- 输入键盘 -->
    <GridView
        android:id="@+id/gv_keybord"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/keyboad_bg"
        android:horizontalSpacing="@dimen/customkeyboard_horizontal"
        android:numColumns="3"
        android:verticalSpacing="@dimen/customkeyboard_vertical" />

    <View
        android:id="@+id/line"
        android:layout_width="match_parent"
        android:layout_height="@dimen/customkeyboard_line"
        android:layout_above="@id/gv_keybord"
        android:background="@color/keyboad_bg" />

    <View
        android:id="@+id/line1"
        android:layout_width="match_parent"
        android:layout_height="@dimen/customkeyboard_line"
        android:layout_below="@id/gv_keybord"
        android:background="@color/keyboad_bg" />

</RelativeLayout>